# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scripts de teste (uso local, não versionar)
scripts/test-*.mjs

# Backup do `configurar-auth-supabase.mjs`, regravado a cada execução.
#
# Não tem segredo — são os 17 campos de política e template que o script
# gerencia. Fica de fora por outro motivo: é uma foto do que havia ANTES da
# última execução NESTA máquina, e serve só ao `--restaurar` local.
#
# Versionado, viraria mais um arquivo com cara de "estado do servidor" sem ser.
# É a armadilha que o CLAUDE.md já registra sobre os `APLICAR_NO_SUPABASE_*.sql`
# — foi assim que o vazamento da tabela de preço passou despercebido. A verdade
# do que está no ar se lê da API, não de um arquivo no repositório.
scripts/auth-config-backup.json

# Prévias de e-mail geradas por `configurar-auth-supabase.mjs --previa`.
# São só as strings do próprio script despejadas em disco, para abrir no
# navegador. A verdade dos templates é o script; isto é subproduto.
scripts/previa-emails/

# Documentacao do projeto: versionada desde 19/08/2026.
#
# Ficava fora do Git, e o preco apareceu: `AUTH.MD`, `PERFIL-CLINIC-PLUS.md` e
# `EXCECOES-REGISTRADAS.md` — os tres arquivos que o CLAUDE.md declarava como
# fonte da verdade da autenticacao — sumiram numa troca de maquina e nao existem
# mais em disco nenhum.
#
# Para a LGPD isso deixou de ser questao de organizacao: o art. 37 e o principio
# de responsabilizacao se demonstram com documento, e documento que nao sobrevive
# a uma troca de maquina nao demonstra nada.
#
# Conferido antes de versionar: so markdown, 152K no total, sem binario e sem
# credencial. A pasta `catalogo/`, que tinha planilhas, ja nao existe.
design/
.od-skills/

# Supabase local temp files
supabase/.temp/

# Local database exports may contain customer and order data
local-exports/
bucket/

# Vercel local project metadata
.vercel/

# Workflow n8n local-only
n8n/

# Saída dos testes de navegador (Playwright)
test-results/
playwright-report/

# Manifesto das contas de teste (ids de conta; nunca versionar)
scripts/.contas-de-teste.json
